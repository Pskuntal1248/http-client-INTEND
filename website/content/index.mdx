# Introduction to Intend

<Callout type="info">
**Intend is an intent-driven HTTP abstraction layer.** 
Instead of micromanaging headers and connection state, you declare your intent (method, URL, auth strategy, payload), and Intend's execution engine handles the rest—automatically resolving complex protocol headers, idempotency keys, content types, and security contexts at runtime.
</Callout>

## The Problem with Traditional API Workspaces

Every API workspace today forces you to think like a protocol engineer. You manually construct headers, paste auth tokens into every request, and hope you didn't accidentally fire a duplicate payment during a network hiccup.

Intend takes a different approach: **you describe your intent, and the app handles the protocol.**

Select `POST`, choose `Bearer Token` from a dropdown, type your URL, write your JSON body, and hit **SEND**. Intend automatically resolves `Content-Type`, `Authorization`, `Idempotency-Key`, and `X-Request-ID` — no raw headers, no guesswork.

## Why Choose Intend?

<div className="docs-box-grid docs-box-grid-2">
  <div className="docs-box-card">
    <h3 className="docs-box-title">Zero-Header Requests</h3>
    <div className="docs-box-text">
      Headers resolved from your intent automatically.
    </div>
  </div>
  <div className="docs-box-card">
    <h3 className="docs-box-title">Pluggable Auth</h3>
    <div className="docs-box-text">
      Auth strategy selected from a dropdown — once.
    </div>
  </div>
  <div className="docs-box-card">
    <h3 className="docs-box-title">Automatic Idempotency</h3>
    <div className="docs-box-text">
      Stripe-style idempotency with automatic retry.
    </div>
  </div>
  <div className="docs-box-card">
    <h3 className="docs-box-title">Offline & Native</h3>
    <div className="docs-box-text">
      Native JavaFX — no browser, no account, fully offline.
    </div>
  </div>
</div>

### Head-to-Head

| Capability | Postman | Insomnia | Thunder Client | **Intend** |
|---|---|---|---|---|
| Zero-header requests | — | — | — | **Yes** |
| Stripe-style idempotency | — | — | — | **Yes** |
| Browser-like headers | — | — | — | **Yes** |
| Response chaining | Scripts | Plugin | Limited | **One-click** |
| Offline / No account | No | No | Yes | **Yes** |

## Next Steps

Get started by downloading the Intend application tailored for your operating system.

- [Download Intend](/docs/getting-started/download)
- [Quick Start Guide](/docs/quick-start)
